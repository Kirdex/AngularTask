import { ColorPageDirective } from './color-page.directive';

describe('ColorPageDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorPageDirective();
    expect(directive).toBeTruthy();
  });
});
